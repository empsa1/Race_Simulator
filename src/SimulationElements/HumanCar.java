package SimulationElements;

import distributions.Distribution;
import environment.Board;

public class HumanCar extends Car{
    public HumanCar(int id, Board board, Distribution dist) {
        super(id, board, dist);
    }
}
